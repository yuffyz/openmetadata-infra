github_org   = "your-org"
github_repo  = "kiban"          # the repo that hosts the workflows, not the subfolder
region       = "us-east-1"

# Must match the state bucket the workflow uses (the TF_STATE_BUCKET repo
# variable). Optional. Locking is S3-native, so there is no lock table.
state_bucket = "dkc-tfstate"

# Let this bootstrap create the bucket instead of making it by hand. Leave false
# if it already exists -- Terraform cannot adopt a bucket it did not create.
# create_state_bucket = true

# If GitHub OIDC is already configured in this AWS account, reuse it:
# create_oidc_provider = false

# To allow the whole repo (any branch/PR/environment) instead of the tight default:
# subject_claims = ["repo:your-org/kiban:*"]
